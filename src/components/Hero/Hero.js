import React from "react";
import HeroCard from "./HeroCard/HeroCard";

function Hero() {
  return (
    <div className="hero">
      {/* put all the pictures card */}
      <HeroCard />
    </div>
  );
}

export default Hero;
